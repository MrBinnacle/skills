---
"mrbinnacle-skills": patch
---

`README.md` is rewritten. The copy and the composition are new; the facts, the tables, the counts and every CI-locked string are carried across unchanged.

The owner's finding on the previous page was that it read as sales copy rather than as a record. Two non-Anthropic reviewers, prompted to refute rather than approve, reached the same finding independently of each other and of the owner (`docs/audit/t1-readme-greenfield-S425/RESULTS.md`, convergence rows C1 and C3). Their second finding was sharper: the first attempt to fix it substituted one performance for another — studied terseness in place of studied wit — which is why this version was written from a direction brief rather than from the previous draft. The brief, its 74 rules, its 11 halt conditions and the 32 tests it hands to a review pass are recorded in `docs/design/direction-brief-S425.md`; what the writing pass did with them, including the four questions it refused to decide, is in `docs/design/make-log-S425.md`.

The register test is a substitution test. Replace `skills` with `mattpocock/skills` in any sentence on the page; if the sentence still reads true, it says nothing about this collection and it does not ship. Two definitional sentences are kept against that test, and the reason is recorded rather than assumed.

One CI lock is retired, and this is the only assertion that changed. `scripts/test_readme_admission_lead.py` required `## Admission method`, `## Card map` and `## Card evidence` to be the first three H2 sections, in that order, with no other H2 before them. That lock forced the page to open on how a card gets admitted, before a reader had been told what a card is or what one costs. The replacement keeps what the lock was for, which was never the ordering: all three sections must still exist, and `ADMISSION.md` must be linked within the first 40 lines, so the admission material reaches the reader before the fold wherever the section sits. The retirement, its authority and its reason are recorded in the function's own docstring, so the next reader finds a loosened guard with an account attached rather than without one.

Every other assertion in that file is untouched. The evidence table still has to match each card's `EVIDENCE.md` posture and occasion count, the admission section still has to name both live instruments, the card map still has to name all three forms, and the disposition record still has to be linked. `scripts/validate_scoreboard.py` is untouched and passes, including the ruled banner line at all five sites and the controlled-results section that pins which cards the page says are measured.

Two corrections to the page's facts, both measured at source rather than carried from the previous copy. `RETIRED.md` holds two departures and not one: the second card was withdrawn on the policy, which is a different route from retiring against a trigger, and the page now states both and distinguishes them. The three evidence counts in the opening are re-measured and agree with the record.
