---
---

`SECURITY.md` prose only. Corrects a claim that `main` has no required status checks, which stopped being true when the `protect-main` ruleset gained six required contexts. No shipped card changed, so nothing reaches installed users.
