---
"mrbinnacle-skills": minor
---

Adds the landing page source under `site/` (one HTML file, one stylesheet, every value drawn from `assets/tokens.json`, the two banner SVGs by reference) and a GitHub Actions Pages workflow that publishes that directory and nothing else; `site/index.html` joins the first-person surfaces that `scripts/validate_voice_provenance.py` scans. Every visible line on the page is a candidate for the owner to pick, and Pages is not enabled by this change.
