---
"mrbinnacle-skills": minor
---

Adds the landing page source under `site/` (one HTML file, one stylesheet, every value drawn from `assets/tokens.json`, seven Phosphor icons vendored inline under MIT, the receipt glyph as the hero mark, the horizontal lockup as the header) and a GitHub Actions Pages workflow that publishes that directory plus the favicon and the social preview and nothing else; `site/index.html` joins the first-person surfaces that `scripts/validate_voice_provenance.py` scans. Adds the favicon set (`assets/favicon.svg`, `site/favicon-32.png`, `site/apple-touch-icon.png`) and the page's title, description and social meta tags. Rebuilds both lockups around the receipt glyph in place of the retired compact mark, exports `assets/social-preview.png` from its SVG source and records the pair's hashes in `assets/tokens.json`, which closes the last two recorded gaps. Every visible line on the page is a candidate for the owner to pick; the social-preview upload to the repository setting is the owner's step.
