---
---

`README.md`: the shell install route points at the `skills.sh` directory listing.

Documentation only. No package behaviour changes, so this changeset is empty and
the release is not bumped by it.
