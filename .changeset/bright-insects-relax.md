---
"mrbinnacle-skills": patch
---

Add plugin eval cases for `decision-rights` and `subagent-handback`. The pre-flight now verifies each case loads its plugin and that its graders observe the declared trigger direction.
