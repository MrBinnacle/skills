---
---

`package.json` `description` only. The field read "A small, evidence-backed collection of agent skills for real engineering." and now matches the GitHub repository description verbatim. `assets/tokens.json` names this field as the repository-local surface the forge description is written from, so the two strings have to agree. The old string also claimed an audience, which the owner ruled out for the same class of phrase on 2026-08-25. No shipped card changed, so nothing reaches installed users.
