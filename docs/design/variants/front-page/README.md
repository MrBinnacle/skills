# Front-page variants

Five unselected front-page drafts from 2026-08-30, recovered from the branch
`rescue/191-front-page-variants` at commit `c80099c` before that branch was deleted (`#201`). The
branch was 23 commits behind `main` and its own commits — voice-provenance work — had already
landed on `main` through `#193`; these five files were its only unique content, so they were
copied out here rather than left to disappear with the branch.

They are raw material, not a foundation, and not evidence that any question about the front page
was already answered. That is the operator's ruling of 2026-08-25, and it governs every prior
brand and design artifact in this project, these five included. Nothing here settles anything and
nothing here selects anything. The front-page direction belongs to `#62`, which runs its own
Context → Direction → Make → Check pass, and that pass — not a reader's preference among these
files — is where a selection gets made.

| File | What distinguishes it |
|---|---|
| [`variant-1.md`](variant-1.md) | Baseline: no tagline, no admission-instrument paragraph, no evidence table. |
| [`variant-2.md`](variant-2.md) | Baseline plus the tagline "These aren't the Claude Code skills you're looking for." and a paragraph separating the three admission instruments. |
| [`variant-3.md`](variant-3.md) | Drops the sizing paragraph; goes straight into a card map organized by what each card addresses. |
| [`variant-4.md`](variant-4.md) | Leads with evidence discipline — the `OBSERVED`/`DESIGNED` provenance split and the per-card evidence table — ahead of the card map. |
| [`variant-5.md`](variant-5.md) | Leads with Install, expanded with the manifest/O7 obligation and the version-promise note. |
