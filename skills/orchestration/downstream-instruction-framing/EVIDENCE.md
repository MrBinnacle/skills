# EVIDENCE — downstream-instruction-framing

Provenance record per the collection's evidence convention (see top-level README →
"The receipts, explained"). Fields are honest by construction: UNMEASURED means exactly that.

| Field | Value |
|---|---|
| **Origin** | OBSERVED 2026-06-07, a private production project (security handoff): the user caught an "Approved Decisions (Already Made — Do Not Re-Litigate)" header in a handoff document and required correction, then generalized the rule ("there's a time and a place for 'do not re-litigate' but it's never an ALWAYS framing"). The SKILL.md Example is the real before/after pair, identifiers genericized. Full entry: [gotchas.md](gotchas.md) → `[OBSERVED]`. |
| **Occasions counted** | 1 — 2026-06-07 security-handoff incident ([gotchas.md](gotchas.md)); later usage observations are explicitly not efficacy or recurrence evidence. **RECURRENCE-THIN**: under two independently counted occasions, which is the one-off ADMISSION.md criterion 2 refuses; recorded as such by the S295 admission triage in `dispositions/`. |
| **Dispatches recorded** | 109 dispatches, lifetime platform counter, measured 2026-08-24. Demand evidence only: slash + model Skill invocations, summed lifetime, not deduplicated by working occasion, blind to hook-injected and always-loaded firings. Summed lifetime is the chosen reading because the counter predates the per-session delta log (2026-08-16), so a lifetime per-session figure is not derivable. Never recurrence, lift, or worth. |
| **Validated against** | Origin incident only, plus continuous in-house use since (the author's global rules mandate invoking it before drafting any handoff/plan/subagent prompt). No controlled screen or paired eval has been run yet. |
| **Observed in use (self-reported)** | Field observations — usage texture, NOT efficacy evidence (tier defined in the top-level README → "The receipts, explained"). Transcript-mining pass 2026-07-12 over one research project's private session archive; re-checked by a second instance of the same AI system, which catches extraction errors, not self-favoring selection — no independent verification; agent model `claude-fable-5`. The count, with its deflation attached: 10 of 10 sessions in the window 2026-07-04 → 2026-07-12 show exactly one invocation each — a compliance observation consistent with the author's standing invoke-before-drafting mandate, not adoption or efficacy evidence. Strongest observed co-occurrences: (a) three planning artifacts carrying a How-to-Treat framing block plus per-item Revisit-if clauses were each written within minutes of that session's invocation; those clauses are observations of the former card, not validation of its corrected per-decision rule; (b) in one 10-agent research sweep dispatched after an invocation, 9 of 10 dispatch prompts carry the skill's signature framing language ("informed proposals from a less-informed reviewer"; explicit refutation licensing), and the returned reports were followed by three recorded re-grades of the author's own prior claims. The causal link (framing → pushback → re-grades) is not measured; only the invocations, the prompt language, and the artifacts are observable. Correction checked 2026-08-12 against the cited artifacts recorded here; no new efficacy evidence was added. |
| **Screen result** | UNMEASURED. Candidate screen task: ask a stock agent to write a handoff mixing evidence-sensitive recommendations with user-owned constraints; behavioral oracle = does it distinguish evidence access from decision rights, attach revisit conditions only where evidence can change the outcome, and preserve constraints, vs. blanket command or blanket-revision framing? NOTE: this is a *judgment-quality* skill — the oracle is behavioral, not deterministic, so a screen here needs a calibrated judge, which is exactly the harder class of measurement. |
| **Paired verdict** | UNMEASURED. Methodology for any future paired Full-vs-Null run: [skill-harness v0.2 pre-registration](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/v0.2-preregistration.md) (registered 2026-07-10). |
| **Standing cost** | Description ≈ 45 tokens if model-invocable (the author runs it model-invocable with the description always loaded — it must fire *before* drafting begins, which after-the-fact triggers can miss). Body ≈ 7 KB, loads only on invocation. |
| **Enforcement note** | Always-loaded description still relies on model-pull to *act* on it, and in a proactive `/loop` there is no turn to retrieve on — so an author who treats this as mandatory (the origin user's global rules do) cannot lean on the skill layer alone. The author backs it in their own env with two deterministic triggers: a UserPromptSubmit router nudge that surfaces the skill on handoff/plan/ADR/dispatch prompts, and a PreToolUse guard on Write/Edit of such artifacts (hard-block on blanket "do not re-litigate" framing; reminder to classify substantive decisions and add Revisit-if only where evidence could change the outcome) — the "after-the-fact triggers can miss" gap the row above flags, closed on the tool call itself. The published skill is unchanged: the model-invocable discipline reference. Correction re-verified 2026-08-12 against the revised card. |
| **Re-screen trigger** | Next major frontier-model release. If a re-screen (or accumulated observation) shows stock agents defaulting to proposal-mood handoffs with revisit clauses unprompted, this skill should be publicly RETIRED with this record updated — that outcome is a feature of the collection, not a failure of the skill. |
