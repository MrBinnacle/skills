# EVIDENCE — downstream-instruction-framing

Provenance record per the collection's evidence convention (see top-level README →
"The receipts, explained"). Fields are honest by construction: UNMEASURED means exactly that.

| Field | Value |
|---|---|
| **Origin** | OBSERVED 2026-06-07, a private production project (security handoff): the user caught an "Approved Decisions (Already Made — Do Not Re-Litigate)" header in a handoff document and required correction, then generalized the rule ("there's a time and a place for 'do not re-litigate' but it's never an ALWAYS framing"). The SKILL.md Example is the real before/after pair, identifiers genericized. Full entry: [gotchas.md](gotchas.md) → `[OBSERVED]`. |
| **Validated against** | Origin incident only, plus continuous in-house use since (the author's global rules mandate invoking it before drafting any handoff/plan/subagent prompt). No controlled screen or paired eval has been run yet. |
| **Observed in use (self-reported)** | Field observations — usage texture, NOT efficacy evidence (tier defined in the top-level README → "The receipts, explained"). Transcript-mining pass 2026-07-12 over one research project's private session archive; re-checked by a second instance of the same AI system, which catches extraction errors, not self-favoring selection — no independent verification; agent model `claude-fable-5`. The count, with its deflation attached: 10 of 10 sessions in the window 2026-07-04 → 2026-07-12 show exactly one invocation each — a compliance observation consistent with the author's standing invoke-before-drafting mandate, not adoption or efficacy evidence. Strongest observed co-occurrences: (a) three planning artifacts carrying the skill's prescribed structure (How-to-Treat framing block + per-item Revisit-if clauses) were each written within minutes of that session's invocation; (b) in one 10-agent research sweep dispatched after an invocation, 9 of 10 dispatch prompts carry the skill's signature framing language ("informed proposals from a less-informed reviewer"; explicit refutation licensing), and the returned reports were followed by three recorded re-grades of the author's own prior claims. The causal link (framing → pushback → re-grades) is not measured; only the invocations, the prompt language, and the artifacts are observable. |
| **Screen result** | UNMEASURED. Candidate screen task: ask a stock agent to write a handoff for a partially-explored plan; behavioral oracle = does the artifact license downstream disagreement and attach revisit conditions, vs. blanket command framing? NOTE: this is a *judgment-quality* skill — the oracle is behavioral, not deterministic, so a screen here needs a calibrated judge, which is exactly the harder class of measurement. |
| **Paired verdict** | UNMEASURED. Methodology for any future paired Full-vs-Null run: [skill-harness v0.2 pre-registration](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/v0.2-preregistration.md) (registered 2026-07-10). |
| **Standing cost** | Description ≈ 45 tokens if model-invocable (the author runs it model-invocable with the description always loaded — it must fire *before* drafting begins, which after-the-fact triggers can miss). Body ≈ 7 KB, loads only on invocation. |
| **Enforcement note** | Always-loaded description still relies on model-pull to *act* on it, and in a proactive `/loop` there is no turn to retrieve on — so an author who treats this as mandatory (the origin user's global rules do) cannot lean on the skill layer alone. The author backs it in their own env with two deterministic triggers: a UserPromptSubmit router nudge that surfaces the skill on handoff/plan/ADR/dispatch prompts, and a PreToolUse guard on Write/Edit of such artifacts (hard-block on blanket "do not re-litigate" framing; reminder when a substantive artifact carries no Revisit-if clause) — the "after-the-fact triggers can miss" gap the row above flags, closed on the tool call itself. The published skill is unchanged: the model-invocable discipline reference. |
| **Re-screen trigger** | Next major frontier-model release. If a re-screen (or accumulated observation) shows stock agents defaulting to proposal-mood handoffs with revisit clauses unprompted, this skill should be publicly RETIRED with this record updated — that outcome is a feature of the collection, not a failure of the skill. |
