# EVIDENCE — parallel-review-disposition-schema

Provenance record per the collection's evidence convention (see top-level README →
"The receipts, explained"). Fields are honest by construction: UNMEASURED means exactly that.

| Field | Value |
|---|---|
| **Origin** | OBSERVED 2026-05-29, a personal production project (phase-2 security-audit lock): a 12-seat parallel council fire (6 security postures + Architect + Planner + 4 domain SMEs) run WITH this schema synthesized into one clean 8-section document with five cleanly-classified disagreements and two cross-builds caught at synthesis. A PRIOR fire on the same corpus with loose free-form prompts had produced a false "agreement" (shared word, divergent scopes) and deferred a re-scope — the exact failures the schema removes. Details: SKILL.md → Verification. |
| **Validated against** | The origin contrast (loose-prompt fire vs schema fire, same corpus) plus a second full instantiation: 2026-07-10, a 4-seat × 72-item library-disposition audit — all seats returned joinable blocks + `status: nominal`, synthesis was mechanical, and one hub cross-build (a wrong-lever recommendation) was caught exactly at the verification step the schema prescribes. |
| **Screen result** | UNMEASURED, structurally: this is a process-discipline skill; its home domain (multi-agent output joinability) has no deterministic sandbox oracle, so the Full-vs-Null screen protocol does not apply. Evidence class is before/after instantiation records (above), which are weaker than paired verdicts — stated plainly. |
| **Paired verdict** | UNMEASURED (see Screen result — not oracle-compatible). Methodology reference: [skill-harness v0.2 pre-registration](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/v0.2-preregistration.md). |
| **Standing cost** | Description ≈ 100 tokens if model-invocable (the dispatch moment is model-side — an orchestrator mid-plan — which is the argument for paying it). Body 6.1 KB + 1 template. |
| **Re-screen trigger** | A harness/runtime shipping native structured multi-agent output contracts would subsume the schema's mechanics (the discipline notes would survive); otherwise re-validate by recording each new instantiation in gotchas.md — an instantiation that produces unjoinable outputs is the falsifier. |
