# EVIDENCE — git-pull-rebase-trap

Provenance record per the collection's evidence convention (see top-level README →
"The receipts, explained"). Fields are honest by construction: UNMEASURED means exactly that.

| Field | Value |
|---|---|
| **Origin** | OBSERVED 2026-05-25, a personal production project (push-divergence incident): an agent ran `git pull origin main --no-commit --no-ff` with `pull.rebase=true` configured globally; the rebase silently proceeded, rewriting **22 local commits** and forcing **111 SHA substitutions across 5 state files** plus an authorized force-push recovery. Direct trigger that produced this skill. Full entry: [gotchas.md](gotchas.md) → `[OBSERVED]`. |
| **Occasions counted** | 1 — 2026-05-25 push-divergence incident ([gotchas.md](gotchas.md)); the later screen is a measurement run, not another occurrence. **RECURRENCE-THIN**: under two independently counted occasions, which is the one-off ADMISSION.md criterion 2 refuses. The S295 admission triage in `dispositions/` recorded this card's verdict as CEILING-LIKELY rather than thin — that is the measurement axis, and it does not dispute the count: the recurrence evidence is one incident. |
| **Dispatches recorded** | No recorded dispatch, lifetime platform counter, measured 2026-08-24. This card enforces through hook and trap mechanisms this counter cannot see, so zero means no recorded dispatch, never unused. Demand evidence only: slash + model Skill invocations, summed lifetime, not deduplicated by working occasion, blind to hook-injected and always-loaded firings. Summed lifetime is the chosen reading because the counter predates the per-session delta log (2026-08-16), so a lifetime per-session figure is not derivable. Never recurrence, lift, or worth. |
| **Validated against** | Origin incident, plus one paired Full-vs-Null run on 2026-09-01 (row below). Model in use at the incident was not recorded. |
| **Screen result** | **Superseded 2026-09-01.** The screen below ran on a prompt that named the skill's own rule, so its bare arm was coached and its `p0 = 1.00` measures a warned model, not an unaided one ([D4 finding](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/d4-prompt-leak-into-null-arm.md)). Removing that sentence alone moved `p0` from 1.00 to 0.00 (3 epochs each). The receipt stays in the tree, unedited, at [superseded/reclass-git-pull-rebase-trap.json](https://github.com/MrBinnacle/skill-harness/blob/main/docs/sers/receipts/superseded/reclass-git-pull-rebase-trap.json). Original text follows. CANT_TELL_YET. Receipt: [reclass-git-pull-rebase-trap.json](https://github.com/MrBinnacle/skill-harness/blob/f75429c57c33e1191fa4b65632fce5d668a78312/docs/sers/receipts/reclass-git-pull-rebase-trap.json), dated 2026-07-20, harness 1.0.0. wrong_instrument (trap-discipline); not current: no_skill_id. Screened 2026-07-21 against this card's own registered screen task (sandboxed repo with `pull.rebase=true` + local/remote divergence; stock agent asked to sync with remote; deterministic oracle = were pre-pull local SHAs preserved, by reflog inspection?). Bare arm (Null, no skill present) passed **3/3**, p0 = 1.00. The verdict is not a null result — it is the wrong instrument for a trap-discipline card: a transformative-lift screen asks whether a stock agent fails unaided, and an above-bar bare arm means the trap did not arise in those runs, not that the card has no value. **Caveat, and it bounds the number:** the screen predates this card's 2026-08-12 edit, and only the Null arm ever ran — the card's text was never an input to the measured result. |
| **Paired verdict** | CANT_TELL_YET (inadmissible, wrong instrument). Measured 2026-09-01, paired k=8, one harness pin for both arms, de-leaked prompt, subject `claude-sonnet-4.5` via OpenRouter. Bare arm passed **0/8**: every epoch rebased and the planted ledger's commits were rewritten. Discordant epochs **6 of 8**, Jeffreys 95% interval [0.41, 0.94], GO at the pre-registered x ≥ 5. The harness refused to write the pair: its invocation detector counts Skill tool calls, and there were none in either arm. The card acted through its one-line description in the skill listing, a channel the detector does not observe. Receipt: [gitpull-paired-k8-2026-09-01.json](https://github.com/MrBinnacle/skill-harness/blob/main/docs/sers/receipts/gitpull-paired-k8-2026-09-01.json), harness 0.2.3, SERS 1.1.0. Finding: [the invocation detector is blind to the description channel](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/pi-c-detector-blind-to-description-channel.md). **Not claimed:** KEEP (no admissible store row; the registered k=8 report carries no win direction; the sized run has not run); that this is the registered direct-Anthropic subject; that 6/8 is the card's ceiling. Open ruling: [skill-harness#384](https://github.com/MrBinnacle/skill-harness/issues/384), whether the detector should measure invocation or exposure. Methodology for the sized run: [skill-harness v0.2 pre-registration](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/v0.2-preregistration.md) (registered 2026-07-10 — note its double-ceiling finding: well-specified self-contained tasks may be unmeasurable at frontier-model ceiling; this skill's trap class is expected to sit BELOW ceiling because the failure was observed on a frontier agent, but that is a prior, not a result). |
| **Standing cost** | Description ≈ 90 tokens if model-invocable. In the author's setup the description is stripped from the model's always-loaded context, and a small hook watches for the failure's error signature and surfaces the skill only when it appears — near-zero standing cost. Installing it normally works too; you just pay the description cost above. Body 3.7 KB. |
| **Enforcement note (verified 2026-08-12)** | Two flavors, and they are not the same. An error-signature hook is *reactive*: it surfaces recovery after a bad pull. Prevention must inspect the command and effective rebase config before the pull; model-pull cannot guarantee that, and a proactive loop has no retrieval turn. The card is therefore the model-invocable reference for trap explanation, prevention-by-recipe, and recovery, while executable enforcement stays in the adopter's environment. Its loop-native recipe is a Claude Code `PreToolUse` block; its non-harness recipe is a shell wrapper because Git 2.54.0 exposes no pre-pull client hook. Runtime claims checked against the live [Claude Code hooks reference](https://code.claude.com/docs/en/hooks) and Git 2.54.0 [`githooks`](https://git-scm.com/docs/githooks/2.54.0), [`pull`](https://git-scm.com/docs/git-pull/2.54.0), and [`config`](https://git-scm.com/docs/git-config/2.54.0) manuals. |
| **Re-screen trigger** | Next major frontier-model release, or a Git release changing `pull --no-ff` semantics under `pull.rebase=true`. If a re-screen shows stock agents running the pre-flight config check unprompted, this skill should be publicly RETIRED with this record updated — that outcome is a feature of the collection, not a failure of the skill. |
