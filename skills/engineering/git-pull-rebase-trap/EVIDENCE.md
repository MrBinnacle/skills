# EVIDENCE — git-pull-rebase-trap

Provenance record per the collection's evidence convention (see top-level README →
"The receipts, explained"). Fields are honest by construction: UNMEASURED means exactly that.

| Field | Value |
|---|---|
| **Origin** | OBSERVED 2026-05-25, a personal production project (push-divergence incident): an agent ran `git pull origin main --no-commit --no-ff` with `pull.rebase=true` configured globally; the rebase silently proceeded, rewriting **22 local commits** and forcing **111 SHA substitutions across 5 state files** plus an authorized force-push recovery. Direct trigger that produced this skill. Full entry: [gotchas.md](gotchas.md) → `[OBSERVED]`. |
| **Occasions counted** | 1 — 2026-05-25 push-divergence incident ([gotchas.md](gotchas.md)); the later screen is a measurement run, not another occurrence. **RECURRENCE-THIN**: under two independently counted occasions, which is the one-off ADMISSION.md criterion 2 refuses. The S295 admission triage in `dispositions/` recorded this card's verdict as CEILING-LIKELY rather than thin — that is the measurement axis, and it does not dispute the count: the recurrence evidence is one incident. |
| **Dispatches recorded** | No recorded dispatch, lifetime platform counter, measured 2026-08-24. This card enforces through hook and trap mechanisms this counter cannot see, so zero means no recorded dispatch, never unused. Demand evidence only: slash + model Skill invocations, summed lifetime, not deduplicated by working occasion, blind to hook-injected and always-loaded firings. Summed lifetime is the chosen reading because the counter predates the per-session delta log (2026-08-16), so a lifetime per-session figure is not derivable. Never recurrence, lift, or worth. |
| **Validated against** | Origin incident, plus two paired Full-vs-Null runs: k=8 on 2026-09-01 and the sized n=32 on 2026-09-03 (row below). Model in use at the incident was not recorded. |
| **Screen result** | CANT_TELL_YET. **Superseded 2026-09-01.** The screen below ran on a prompt that named the skill's own rule, so its bare arm was coached and its `p0 = 1.00` measures a warned model, not an unaided one ([D4 finding](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/d4-prompt-leak-into-null-arm.md)). Removing that sentence alone moved `p0` from 1.00 to 0.00 (3 epochs each). The receipt stays in the tree, unedited, at [superseded/reclass-git-pull-rebase-trap.json](https://github.com/MrBinnacle/skill-harness/blob/0af2f992adf7caa7259cb649cafd48ad0e171395/docs/sers/receipts/superseded/reclass-git-pull-rebase-trap.json). Original text follows. Receipt: [reclass-git-pull-rebase-trap.json](https://github.com/MrBinnacle/skill-harness/blob/f75429c57c33e1191fa4b65632fce5d668a78312/docs/sers/receipts/reclass-git-pull-rebase-trap.json), dated 2026-07-20, harness 1.0.0. wrong_instrument (trap-discipline); not current: no_skill_id. Screened 2026-07-21 against this card's own registered screen task (sandboxed repo with `pull.rebase=true` + local/remote divergence; stock agent asked to sync with remote; deterministic oracle = were pre-pull local SHAs preserved, by reflog inspection?). Bare arm (Null, no skill present) passed **3/3**, p0 = 1.00. The verdict is not a null result — it is the wrong instrument for a trap-discipline card: a transformative-lift screen asks whether a stock agent fails unaided, and an above-bar bare arm means the trap did not arise in those runs, not that the card has no value. **Caveat, and it bounds the number:** the screen predates this card's 2026-08-12 edit, and only the Null arm ever ran — the card's text was never an input to the measured result. |
| **Paired verdict** | CANT_TELL_YET. Receipt: [gitpull-paired-n32-2026-09-03-sized.json](https://github.com/MrBinnacle/skill-harness/blob/0af2f992adf7caa7259cb649cafd48ad0e171395/docs/sers/receipts/gitpull-paired-n32-2026-09-03-sized.json), dated 2026-09-03, harness 0.2.3. **Typed reason: hazard not met.** The sized paired run (n = 32, subject `claude-sonnet-5`, de-leaked prompt) passed every epoch in both arms: lattice both_pass 32, full_only 0, null_only 0, both_fail 0; signed delta 0.000, 95% CI [-0.107, 0.107]. Zero of 32 Null epochs and zero of 32 Full epochs ran `git pull`; all 64 fetched and merged, so the armed `pull.rebase=true` configuration was never exercised. Under the [skill-harness#403](https://github.com/MrBinnacle/skill-harness/issues/403) ruling (2026-09-03, section 4) a Null arm that never enters the hazard fails the fixture's qualification, so this run is not an observation of the trap-discipline estimand. It is a finding about the task, which does not present the hazard to this subject, and not about the card. [skill-harness#421](https://github.com/MrBinnacle/skill-harness/issues/421) makes `evaluate-paired` refuse this shape by name (`HAZARD_NOT_MET`); this receipt is the last read taken under the pre-#421 instrument. Currency gate run 2026-09-04 ([skills#219](https://github.com/MrBinnacle/skills/issues/219)): card content, harness identity, arm coverage and trigger attestation pass. **Not claimed:** KEEP; CUT; a win direction; that the card's guard is inert — the untreated agent did not pull on this task, so the guard was never tested. **Next admissible read:** a Null screen on the priced subject that qualifies a hazard-enriched task ([skill-harness#419](https://github.com/MrBinnacle/skill-harness/issues/419) item 3), which carries a spend no dated authorisation yet covers. **Preceding micro-run, kept as history:** measured 2026-09-01, paired k=8, one harness pin for both arms, de-leaked prompt, subject `claude-sonnet-4.5` via OpenRouter; CANT_TELL_YET (underpowered), admissible. Bare arm passed **0/8**: every epoch rebased and the planted ledger's commits were rewritten. Discordant epochs **6 of 8**, Jeffreys 95% interval [0.41, 0.94], GO at the pre-registered x ≥ 5. The two subjects differ in kind: Sonnet 4.5 pulled in 8 of 8 untreated epochs and Sonnet 5 in 0 of 32, so the k=8 GO datum did not transfer to the sized run's subject, and that is why the sized run met no hazard. k=8 receipt: [gitpull-paired-k8-2026-09-01-detector-v2.json](https://github.com/MrBinnacle/skill-harness/blob/0af2f992adf7caa7259cb649cafd48ad0e171395/docs/sers/receipts/superseded/gitpull-paired-k8-2026-09-01-detector-v2.json), harness 0.2.3, SERS 1.2.0. **Delivery: description only.** The card's description was present in 8 of 8 treated epochs and 0 of 8 bare epochs; Skill tool invocations were 0 of 8 (π̂_c = 0.00, 95% CI [0.00, 0.37]). The card changed the agent's git command through its one-line description in the skill listing; its body never loaded. **Supersession, 2026-09-02.** The first receipt for this run recorded these same measurements as *inadmissible*: the harness's detector counted only Skill tool calls, saw none, and refused the write. [skill-harness#384](https://github.com/MrBinnacle/skill-harness/issues/384) ruled that exposure is the treatment and invocation a stratifier; the pair then ingested without refusal. The refusal was an instrument defect, not a property of the evidence. The 1.1.0 receipt stays in the tree unedited at [superseded/gitpull-paired-k8-2026-09-01.json](https://github.com/MrBinnacle/skill-harness/blob/0af2f992adf7caa7259cb649cafd48ad0e171395/docs/sers/receipts/superseded/gitpull-paired-k8-2026-09-01.json). Finding: [the invocation detector is blind to the description channel](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/pi-c-detector-blind-to-description-channel.md). Neither run carries a Gate-2 verdict (that read binds to a ratified design record, and none exists yet), and neither is the registered direct-Anthropic subject. Methodology for the sized run: [skill-harness v0.2 pre-registration](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/v0.2-preregistration.md) (registered 2026-07-10 — note its double-ceiling finding: well-specified self-contained tasks may be unmeasurable at frontier-model ceiling; this skill's trap class is expected to sit BELOW ceiling because the failure was observed on a frontier agent, but that is a prior, not a result). |
| **Standing cost** | Description ≈ 90 tokens if model-invocable. In the author's setup the description is stripped from the model's always-loaded context, and a small hook watches for the failure's error signature and surfaces the skill only when it appears — near-zero standing cost. Installing it normally works too; you just pay the description cost above. Body 3.7 KB. |
| **Enforcement note (verified 2026-08-12)** | Two flavors, and they are not the same. An error-signature hook is *reactive*: it surfaces recovery after a bad pull. Prevention must inspect the command and effective rebase config before the pull; model-pull cannot guarantee that, and a proactive loop has no retrieval turn. The card is therefore the model-invocable reference for trap explanation, prevention-by-recipe, and recovery, while executable enforcement stays in the adopter's environment. Its loop-native recipe is a Claude Code `PreToolUse` block; its non-harness recipe is a shell wrapper because Git 2.54.0 exposes no pre-pull client hook. Runtime claims checked against the live [Claude Code hooks reference](https://code.claude.com/docs/en/hooks) and Git 2.54.0 [`githooks`](https://git-scm.com/docs/githooks/2.54.0), [`pull`](https://git-scm.com/docs/git-pull/2.54.0), and [`config`](https://git-scm.com/docs/git-config/2.54.0) manuals. |
| **Re-screen trigger** | Next major frontier-model release, or a Git release changing `pull --no-ff` semantics under `pull.rebase=true`. If a re-screen shows stock agents running the pre-flight config check unprompted, this skill should be publicly RETIRED with this record updated — that outcome is a feature of the collection, not a failure of the skill. **Attested 2026-09-04:** no named trigger has fired since 2026-09-03; checked the `git/git` tag list (newest `v2.55.0`, committed 2026-06-29, before the receipt) and the Anthropic model roster (no model released after the receipt's subject `claude-sonnet-5`). |
