# EVIDENCE — success-test-accepts-any-output

Provenance record per the collection's evidence convention (see top-level README →
"The receipts, explained"). Fields are honest by construction: UNMEASURED means exactly that.

| Field | Value |
|---|---|
| **Origin** | OBSERVED 2026-08-17, during a GitHub GraphQL outage. A retry loop whose success test was `[ -n "$url" ]` printed `#44 OK {"message":"No server is currently available..."}` for two targets while zero comments posted, because `gh` wrote the API error body to stdout and an error body is non-empty. In the same session a test harness comparing `String(got) === String(want)` accepted a value of the wrong type. Both defects sat inside verification machinery, in a repository whose product detects false-green reporting. Details: SKILL.md → Example. |
| **Occasions counted** | 2 — 2026-08-17 origin, a retry loop and a test harness both accepting failure output as success (gotchas.md); 2026-08-23 the first instance in the negative direction, where a probe harness invoked a filename that did not exist, printed nothing for all six probes, and reported the absence as a finding (gotchas.md). The retry loop and the test harness are counted as one occasion, not two: they are fan-out from a single session, which ADMISSION.md criterion 2 refuses to inflate. |
| **Dispatches recorded** | No recorded dispatch, measured 2026-08-24. The figure is a tautology and is stated only so the row is not blank: this card sat in `_quarantine/` until 2026-08-24, so it was never installed and the platform counter had nothing it could have counted. It is not evidence about demand, recurrence or worth. Re-measure after one release cycle in the published tree. |
| **Validated against** | Each remedy was proven against the failure that produced it. Rule 1 and rule 3 were validated by re-reading the comment lists and confirming neither comment had posted, which is what the loop's own output had denied. Rule 2 was validated as a behaviour-preserving swap: two suites held at exactly 52 and 73 assertions with zero failures across the replacement of stringified comparison with identity comparison, so nothing had been relying on the looseness. Rule 4 was added from the 2026-08-23 instance and is validated by the mechanism that caught it — two known-good fixtures returning negative, which is not a shape a real predicate gap takes. |
| **Screen result** | UNMEASURED. The card is a review lens over verification code rather than a task with a deterministic sandbox oracle, so the Full-vs-Null screen protocol does not apply as written. The evidence class is three dated instances across two projects, with the remedy for each proven against the failure that produced it — weaker than a paired verdict, and stated as such. |
| **Paired verdict** | UNMEASURED (see Screen result — not oracle-compatible). Methodology reference: [skill-harness v0.2 pre-registration](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/v0.2-preregistration.md). |
| **Standing cost** | Description ≈ 45 tokens, paid on every turn while the card is model-invocable. The retrieval moment is model-side — an agent writing a success predicate, or about to believe one — which is the argument for paying it. Body 7,031 B (measured 2026-08-24), loaded only on retrieval. |
| **Re-screen trigger** | The card carries one claim that is reproduced rather than specified: that `gh` writes API error bodies to stdout. The published `gh api` manual documents `--silent` but does not state which stream an error body uses. If a future manual specifies the stream, or `gh` changes it, that half of the card is re-dated or corrected against the manual. The card as a whole retires when a test runner or shell reports which predicates cannot be made red by any input, because that makes the decorative check visible without the review. |
