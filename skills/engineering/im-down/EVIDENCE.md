# EVIDENCE — im-down

Provenance record per the collection's evidence convention (see top-level README →
"The receipts, explained"). Fields are honest by construction: UNMEASURED means exactly that.

| Field | Value |
|---|---|
| **Origin** | DESIGNED 2026-08-09 → 2026-08-10. Built on purpose, not after an incident: the author's old session-close ritual wasn't good enough, so they designed a replacement over those two days and had it built. Most skills here exist because something went wrong; this one and `im-up` exist because the author wanted them. There is a real problem behind it — a close step you could skip left more than one session with no packet at all ([gotchas.md](gotchas.md) → `[OBSERVED 2026-08-07]`). |
| **Occasions counted** | 2 — 2026-08-07 skipped-close occurrence ([gotchas.md](gotchas.md)); 2026-08-23 out-of-pipeline session that closed with no packet produced ([gotchas.md](gotchas.md)). Design and fixture work are not independent occurrences. The two are not fan-out from each other: they sit sixteen days and roughly twenty-eight sessions apart, in different repositories, by different cause paths — the first a skipped step, the second a session that never entered the pipeline at all. The second was found by reading the session record, not by running the skill. The S295 admission triage in `dispositions/` recorded this card as thin on the day it ran; that dated record stands as written and is not rewritten here. |
| **Dispatches recorded** | 56 dispatches, lifetime platform counter, measured 2026-08-24. Demand evidence only: slash + model Skill invocations, summed lifetime, not deduplicated by working occasion, blind to hook-injected and always-loaded firings. Summed lifetime is the chosen reading because the counter predates the per-session delta log (2026-08-16), so a lifetime per-session figure is not derivable. Never recurrence, lift, or worth. |
| **Validated against** | The author's own production session-end and resume ritual skills, read in full 2026-08-09 (private repo; commit pinned in the source tree). Then reviewed by reproduction on 2026-08-10: four verification holes were found against throwaway git repos and fixed ([gotchas.md](gotchas.md) → the `[OBSERVED 2026-08-10]` entries). Nine fixture classes pass — clean, stale, incomplete, failed-probe, placeholder, unfailable-check, command-probe, receive-mode-config, no-drift — run by CI on Linux and Windows since 2026-08-10. |
| **Screen result** | UNMEASURED. The task this row previously registered as its "screen task" ran on 2026-08-23 and passed: a packet written at one session's close was received at the next session's cold start, verdict `ACCEPTED`, branch and HEAD matched, all seven repository-configured receiver checks exited 0, both `command` probes executed from the repository's own trusted list, and the durable checkpoint the old ritual writes was read intact at the same start. **That is a cutover acceptance and it is not a screen.** A screen is a with-and-without comparison against a model working unaided; this ran the skill only, so it cannot say what would have happened without it. Registering it in this row was a category error, corrected here rather than in place — the acceptance is real, the word "screen" was wrong. This row stays UNMEASURED because no with-and-without comparison has been run. Also worth stating: two claims that packet labelled `unverified` were false when checked at source ([gotchas.md](gotchas.md) → the second `[OBSERVED 2026-08-23]` entry). Acceptance covers `verified` claims, branch, HEAD and the repository's checks. It is not a warrant for the packet's prose. |
| **Paired verdict** | UNMEASURED. Method for any future paired Full-vs-Null run: [skill-harness v0.2 pre-registration](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/v0.2-preregistration.md). No prior claimed either way. |
| **Standing cost** | Zero standing context cost. Ships `disable-model-invocation: true`, so it is human-invoked and its description never sits in the model's always-on context. Body 6,408 bytes, measured 2026-09-06 (this row said 2.6 KB from an earlier revision); `PACKET-FORMAT.md`, the validator and the fixtures load only when you invoke it. |
| **Size disposition** | 6,408 bytes on 2026-09-06, above the 5,120-byte target and under the 7,168-byte maximum, reviewed under the amended Sizes rule (`AGENTS.md`). The card was 5,211 bytes before `skills#254`; the safe-to-clear verdict (`skills#248`) added the five checks inline in Boundary limits. Shape chosen: inline, with no auxiliary file. The checks are the producer's contract for the one irreversible action at the boundary, every run reaches them, and a pointer to a sibling file would put the most important line of the card behind a fetch. The other two shapes were considered: an auxiliary with a read instruction disclosed contract as if it were optional reference; moving the procedure into `im-up` put a producer obligation on the receiver card. Duplication review: none found; the `close_session.py` and `snapshot_state.py` sections describe two different callers. Nothing was cut to approach the target. Revisit if the card crosses 7,168 bytes, or if the boundary contract moves the verdict to the receiver side. |
| **Enforcement note** | It only fires when a human invokes it — the same gap its origin names, a close step you can skip. The skill does not fix that. The real check is at the other end: `im-up` validates the packet and rejects a stale or incomplete one (exit 2), so a skipped or bad close shows up at the next session's start. If you need the close itself to fire, back it with a hook in your own setup. |
| **Re-screen trigger** | Claude Code ships a native atomic state packet with receiver verification and abnormal-exit coverage. If that happens, retire this skill publicly with this record intact. |
