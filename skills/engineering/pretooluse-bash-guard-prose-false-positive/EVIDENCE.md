# EVIDENCE — pretooluse-bash-guard-prose-false-positive

Provenance record per the collection's evidence convention (see top-level README →
"The receipts, explained"). Fields are honest by construction: UNMEASURED means exactly that.

| Field | Value |
|---|---|
| **Origin** | OBSERVED 2026-08-17: a newly written `PreToolUse` Bash guard blocked the very commit that installed it. The commit message described the CLI the guard policed and quoted the banned wording as an example, so the guard matched its own documentation. Two independent defects in one commit — an unanchored detection regex, and a predicate applied to a heredoc body that no rule about *running* a program should read. Details: SKILL.md → Problem. |
| **Occasions counted** | 5 — 2026-08-17 origin, a guard blocking its own installing commit (SKILL.md → Problem); 2026-08-18 a third project and a third guard family, a rulebook plugin failing closed on a long `gh issue create` heredoc (gotchas.md); 2026-08-23 a corpus-search guard blocking a pull-request body and then a commit message, both on the bare verb `find` (gotchas.md); 2026-08-24 two reproductions on a read-whole rule, where Python locals named `head` and `tail` inside a heredoc read as pager commands and the commit describing that block was itself blocked (gotchas.md); 2026-08-24 (recorded 2026-08-25) three blocks in one external session on three different guards, all fired by prose - two commit bodies and a JSON payload - with predicate names not preserved, stated as a limit (gotchas.md). |
| **Dispatches recorded** | No recorded dispatch, measured 2026-08-24. The figure is a tautology and is stated only so the row is not blank: this card sat in `_quarantine/` until 2026-08-24, so it was never installed and the platform counter had nothing it could have counted. It is not evidence about demand, recurrence or worth. Re-measure after one release cycle in the published tree, when the zero becomes informative. |
| **Validated against** | Both fixes were proven as before/after pairs on the verbatim commands that had been blocked. Heredoc-payload stripping plus command-position anchoring: the two previously-blocked prose commands pass, while a bare `find` over the corpus, a `cd <corpus> && find .`, a `find` after a pipe, after an env assignment, and under `xargs` all still block. Eight fixtures added including both prose bodies verbatim; suite 97/97. The 2026-08-24 pair was proven the same way, with poison fixtures reproducing both real incidents RED before the fix and passing after it, and the real pager invocation still blocking. |
| **Screen result** | UNMEASURED. The card's subject is hook-authoring judgement rather than a task with a deterministic sandbox oracle, so the Full-vs-Null screen protocol does not apply as written. The evidence class is five dated incidents across three projects and at least four distinct guards (the fifth incident's three predicates are unnamed in its record), plus before/after fixture pairs — weaker than a paired verdict, and stated as such. |
| **Paired verdict** | UNMEASURED (see Screen result — not oracle-compatible). Methodology reference: [skill-harness v0.2 pre-registration](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/v0.2-preregistration.md). |
| **Standing cost** | Description ≈ 50 tokens, paid on every turn while the card is model-invocable. The retrieval moment is model-side — an agent writing or debugging a hook — which is the argument for paying it. Body 4,395 B (measured 2026-08-24), loaded only on retrieval. |
| **Re-screen trigger** | A `PreToolUse` payload that delivers the parsed command structure, or the heredoc bodies separated from the shell, rather than one flat command string. That platform change makes the failure impossible to write, and the card retires against this criterion with no screen required. Short of that, each new instance is appended to gotchas.md; an instance where command-position anchoring and heredoc stripping fail to prevent the false positive is the falsifier. |
