# EVIDENCE — router-skill-predicate-gap

Provenance record per the collection's evidence convention (see top-level README →
"The receipts, explained"). Fields are honest by construction: UNMEASURED means exactly that.

| Field | Value |
|---|---|
| **Origin** | OBSERVED 2026-08-18. A machine-level rule file marked a skill as router-enforced and MANDATORY before any handoff, plan, ADR or subagent prompt. The bare word `plan` was absent from the pattern list. The skill had fired earlier that session only because the work also involved an ADR, so a different pattern matched and the correct behaviour was coincidence relative to the rule's stated purpose. Nothing distinguished "the predicate did not match" from "no prompt needed it". Found by piping a prompt into the live hook, not by reading the rule file. Details: SKILL.md → Example. |
| **Occasions counted** | 2 — 2026-08-18 origin, a mandatory rule whose pattern list omitted the ordinary word for the artifact it guarded (gotchas.md); 2026-08-23 a second rule on the same install, where the maintainer's plain request went unmatched and the broadest of four patterns held a literal backspace character where a regex word boundary was meant, so it had matched nothing since the day it shipped (gotchas.md). |
| **Dispatches recorded** | No recorded dispatch, measured 2026-08-24. The figure is a tautology and is stated only so the row is not blank: this card sat in `_quarantine/` until 2026-08-24, so it was never installed and the platform counter had nothing it could have counted. It is not evidence about demand, recurrence or worth. Re-measure after one release cycle in the published tree. |
| **Validated against** | Both instances were proven as before/after pairs on the same prompt string against the same live hook, using the maintainer's own message from the session that exposed the gap rather than an invented probe. The 2026-08-18 fix added three patterns and was checked against five false-positive probes that stayed silent, including words sharing the stem. The 2026-08-23 fix doubled the damaged escape, added a needs/could-use pattern, added four fixtures including two that only the broad pattern can satisfy, and added a class-level assertion refusing any pattern that holds a control character. |
| **Screen result** | UNMEASURED. The card's subject is an audit procedure over a configuration file, and its home domain has no deterministic sandbox oracle, so the Full-vs-Null screen protocol does not apply as written. The evidence class is two dated instances with before/after probe pairs on a live hook — weaker than a paired verdict, and stated as such. |
| **Paired verdict** | UNMEASURED (see Screen result — not oracle-compatible). Methodology reference: [skill-harness v0.2 pre-registration](https://github.com/MrBinnacle/skill-harness/blob/main/docs/findings/v0.2-preregistration.md). |
| **Standing cost** | Description ≈ 45 tokens, paid on every turn while the card is model-invocable. The retrieval moment is model-side — an agent about to write, audit or rely on a router rule — which is the argument for paying it. Body 7.0 KB, loaded only on retrieval. |
| **Re-screen trigger** | A router implementation that reports which pattern matched, and that fails a rule whose pattern is reachable by no fixture. Either makes both failure modes visible without the audit, and the card retires against this criterion. Short of that, each new instance is appended to gotchas.md; an instance where per-pattern fixture coverage and the control-character assertion both hold and the predicate is still silently incomplete is the falsifier. |
