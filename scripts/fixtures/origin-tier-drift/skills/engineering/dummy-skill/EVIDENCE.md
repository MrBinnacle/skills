# EVIDENCE — dummy-skill (fixture)

Fixture record. Its Origin tier is `OBSERVED`; the tree's README claims two `OBSERVED` cards and
no `DISTILLED` one, so the derived tiering and the stated tiering disagree.

Everything else in this tree agrees with the repository — the four scoreboard numbers are
correct and the policy version matches — so the only thing this fixture can fail on is the
origin-tier assertion under test.

| Field | Value |
|---|---|
| **Origin** | OBSERVED 2026-01-01, a fixture incident. |
| **Screen result** | UNMEASURED. Fixture. |
| **Paired verdict** | UNMEASURED. Fixture. |
