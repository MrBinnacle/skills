# EVIDENCE — dummy-skill (fixture)

Fixture record, and the poison in this tree. Its Screen result opens with a word outside the
validator's closed verdict vocabulary. An open test ("anything that is not UNMEASURED counts
as a result") would read it as a measurement that never happened — the direction that flatters
the page — so the validator must refuse it rather than count it either way.

Nothing else in this tree can fail: every banner site carries the ruled line verbatim and the
policy version matches, so the only thing this fixture can go red on is the vocabulary refusal
under test.

| Field | Value |
|---|---|
| **Screen result** | Pending review. Fixture: deliberately outside the closed vocabulary. |
| **Paired verdict** | UNMEASURED. Fixture. |
